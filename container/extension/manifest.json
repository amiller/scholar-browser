{
  "manifest_version": 3,
  "name": "scholar-browser control",
  "version": "0.1.0",
  "description": "Minimal control extension: navigate / evaluate / screenshot, driven over a local HTTP bridge.",
  "permissions": ["tabs", "scripting", "alarms"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background/service-worker.js" }
}
